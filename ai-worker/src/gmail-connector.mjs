const GOOGLE_AUTH='https://accounts.google.com/o/oauth2/v2/auth'
const GOOGLE_TOKEN='https://oauth2.googleapis.com/token'
const GMAIL_API='https://gmail.googleapis.com/gmail/v1/users/me'
const TOKEN_KEY='connector:gmail:tokens:v1'
const STATE_PREFIX='connector:gmail:oauth:'
const SCOPES=['openid','email','https://www.googleapis.com/auth/gmail.readonly','https://www.googleapis.com/auth/gmail.send']
const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'}})
const clean=(v,max=5000)=>String(v??'').replace(/[\u0000-\u001f]/g,' ').trim().slice(0,max)
const b64url=(bytes)=>{let s='';for(const b of bytes)s+=String.fromCharCode(b);return btoa(s).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
const unb64=(s)=>{const p=String(s).replace(/-/g,'+').replace(/_/g,'/');const raw=atob(p+'='.repeat((4-p.length%4)%4));return Uint8Array.from(raw,c=>c.charCodeAt(0))}
function internalAuthorized(request,env){return Boolean(env.INTERNAL_SECRET)&&request.headers.get('X-OTYA-Internal-Secret')===env.INTERNAL_SECRET}
function config(env){return{clientId:env.GMAIL_GOOGLE_CLIENT_ID||'',clientSecret:env.GMAIL_GOOGLE_CLIENT_SECRET||'',enc:env.CONNECTOR_ENCRYPTION_KEY||env.INTERNAL_SECRET||'',base:(env.WEBSITE_URL||'https://petersmartlink.com').replace(/\/$/,'')}}
function configured(env){const c=config(env);return Boolean(c.clientId&&c.clientSecret&&c.enc&&env.KV)}
function redirectUri(env){return`${config(env).base}/api/ai/oauth/google/callback`}
async function cryptoKey(env){const secret=new TextEncoder().encode(config(env).enc);const digest=await crypto.subtle.digest('SHA-256',secret);return crypto.subtle.importKey('raw',digest,{name:'AES-GCM'},false,['encrypt','decrypt'])}
async function seal(env,value){const iv=crypto.getRandomValues(new Uint8Array(12));const data=new TextEncoder().encode(JSON.stringify(value));const encrypted=new Uint8Array(await crypto.subtle.encrypt({name:'AES-GCM',iv},await cryptoKey(env),data));return`${b64url(iv)}.${b64url(encrypted)}`}
async function open(env,value){const[iv,data]=String(value||'').split('.');if(!iv||!data)return null;try{const plain=await crypto.subtle.decrypt({name:'AES-GCM',iv:unb64(iv)},await cryptoKey(env),unb64(data));return JSON.parse(new TextDecoder().decode(plain))}catch{return null}}
async function readTokens(env){const raw=await env.KV?.get(TOKEN_KEY);return raw?open(env,raw):null}
async function saveTokens(env,tokens){await env.KV.put(TOKEN_KEY,await seal(env,tokens))}
async function exchange(env,params){const c=config(env);const body=new URLSearchParams({...params,client_id:c.clientId,client_secret:c.clientSecret});const response=await fetch(GOOGLE_TOKEN,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(clean(data.error_description||data.error||`Google OAuth HTTP ${response.status}`,500));return data}
async function accessToken(env){const tokens=await readTokens(env);if(!tokens)return null;if(tokens.access_token&&Number(tokens.expires_at||0)>Date.now()+60000)return tokens.access_token;if(!tokens.refresh_token)return null;const fresh=await exchange(env,{grant_type:'refresh_token',refresh_token:tokens.refresh_token});const next={...tokens,access_token:fresh.access_token,expires_at:Date.now()+Number(fresh.expires_in||3600)*1000,scope:fresh.scope||tokens.scope};await saveTokens(env,next);return next.access_token}
async function gmailFetch(env,path,init={}){const token=await accessToken(env);if(!token)throw new Error('Gmail is not connected');const response=await fetch(`${GMAIL_API}${path}`,{...init,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json',...(init.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(clean(data?.error?.message||`Gmail HTTP ${response.status}`,500));return data}
function header(message,name){return message?.payload?.headers?.find(h=>String(h.name).toLowerCase()===name.toLowerCase())?.value||''}
function decodeBody(data){if(!data)return'';try{return new TextDecoder().decode(unb64(data))}catch{return''}}
function bodyText(payload){if(!payload)return'';if(payload.mimeType==='text/plain'&&payload.body?.data)return clean(decodeBody(payload.body.data),12000);for(const part of payload.parts||[]){const found=bodyText(part);if(found)return found}return''}
export async function gmailStatus(env){const tokens=configured(env)?await readTokens(env):null;return{configured:configured(env),connected:Boolean(tokens?.refresh_token||tokens?.access_token),email:tokens?.email||null,scopes:SCOPES}}
export async function handleGmailConnector(request,env){const url=new URL(request.url);const callback=url.pathname==='/api/ai/oauth/google/callback';if(!callback&&!internalAuthorized(request,env))return json({error:'Unauthorized'},401);if(!configured(env))return callback?new Response('Gmail connector is not configured',{status:503}):json({error:'Gmail connector setup required',required:['GMAIL_GOOGLE_CLIENT_ID','GMAIL_GOOGLE_CLIENT_SECRET'],optional:['CONNECTOR_ENCRYPTION_KEY']},503);try{
if(url.pathname.endsWith('/status')&&request.method==='GET')return json({ok:true,gmail:await gmailStatus(env)})
if(url.pathname.endsWith('/start')&&request.method==='POST'){const state=b64url(crypto.getRandomValues(new Uint8Array(32)));await env.KV.put(`${STATE_PREFIX}${state}`,'1',{expirationTtl:600});const auth=new URL(GOOGLE_AUTH);auth.searchParams.set('client_id',config(env).clientId);auth.searchParams.set('redirect_uri',redirectUri(env));auth.searchParams.set('response_type','code');auth.searchParams.set('scope',SCOPES.join(' '));auth.searchParams.set('access_type','offline');auth.searchParams.set('prompt','consent');auth.searchParams.set('state',state);auth.searchParams.set('include_granted_scopes','true');return json({ok:true,authorization_url:auth.toString()})}
if(callback&&request.method==='GET'){const state=url.searchParams.get('state')||'';const code=url.searchParams.get('code')||'';if(!state||!code||!(await env.KV.get(`${STATE_PREFIX}${state}`)))return new Response('Invalid or expired OAuth state',{status:400});await env.KV.delete(`${STATE_PREFIX}${state}`);const token=await exchange(env,{grant_type:'authorization_code',code,redirect_uri:redirectUri(env)});const existing=await readTokens(env)||{};const stored={...existing,access_token:token.access_token,refresh_token:token.refresh_token||existing.refresh_token,expires_at:Date.now()+Number(token.expires_in||3600)*1000,scope:token.scope||'',connected_at:new Date().toISOString()};await saveTokens(env,stored);return Response.redirect(`${config(env).base}/admin/ai?gmail=connected`,302)}
if(url.pathname.endsWith('/inbox')&&request.method==='GET'){const max=Math.max(1,Math.min(Number(url.searchParams.get('limit')||10),20));const list=await gmailFetch(env,`/messages?maxResults=${max}&q=${encodeURIComponent('in:inbox')}`);const messages=[];for(const item of list.messages||[]){const m=await gmailFetch(env,`/messages/${encodeURIComponent(item.id)}?format=metadata&metadataHeaders=From&metadataHeaders=Subject&metadataHeaders=Date`);messages.push({id:m.id,threadId:m.threadId,from:clean(header(m,'From'),500),subject:clean(header(m,'Subject'),500),date:clean(header(m,'Date'),200),snippet:clean(m.snippet,500)})}return json({ok:true,messages})}
if(url.pathname.endsWith('/message')&&request.method==='GET'){const id=url.searchParams.get('id');if(!id)return json({error:'id is required'},400);const m=await gmailFetch(env,`/messages/${encodeURIComponent(id)}?format=full`);return json({ok:true,message:{id:m.id,threadId:m.threadId,from:clean(header(m,'From'),500),subject:clean(header(m,'Subject'),500),date:clean(header(m,'Date'),200),text:bodyText(m.payload)||clean(m.snippet,4000)}})}
return json({error:'Not found'},404)}catch(error){console.error('[gmail-connector]',error?.message);return callback?new Response('Gmail connection failed',{status:500}):json({error:'Gmail connector failed',detail:clean(error?.message,400)},500)}}
