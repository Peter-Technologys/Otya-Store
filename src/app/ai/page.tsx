'use client'
import {useEffect,useState} from 'react'
type M={role:'user'|'assistant';content:string}
export default function OtyaAiPage(){const[guest,setGuest]=useState('');const[conversation,setConversation]=useState('');const[messages,setMessages]=useState<M[]>([]);const[input,setInput]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');useEffect(()=>{let g=localStorage.getItem('otya_ai_guest')||'';if(!g){g=crypto.randomUUID()+crypto.randomUUID();localStorage.setItem('otya_ai_guest',g)}setGuest(g);setConversation(localStorage.getItem('otya_ai_conversation')||'')},[]);useEffect(()=>{if(!guest||!conversation)return;let live=true;(async()=>{try{const r=await fetch(`/api/ai/chat?guest_id=${encodeURIComponent(guest)}&conversation_id=${encodeURIComponent(conversation)}`,{cache:'no-store'});const d=await r.json().catch(()=>({}));if(r.ok&&live)setMessages((d.conversation?.messages||[]).map((m:M)=>({role:m.role,content:m.content})))}catch{}})();return()=>{live=false}},[guest,conversation]);async function send(){const text=input.trim();if(!text||!guest||busy)return;setInput('');setMessages(m=>[...m,{role:'user',content:text}]);setBusy(true);setError('');try{const r=await fetch('/api/ai/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,guest_id:guest,conversation_id:conversation||undefined})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'OTYA AI is unavailable');if(d.conversation_id){setConversation(d.conversation_id);localStorage.setItem('otya_ai_conversation',d.conversation_id)}setMessages(m=>[...m,{role:'assistant',content:d.answer||'I could not answer that.'}])}catch(e){setError((e as Error).message)}finally{setBusy(false)}}function newChat(){localStorage.removeItem('otya_ai_conversation');setConversation('');setMessages([]);setError('')}return <main className="min-h-screen flex flex-col" style={{background:'var(--cosmos-scaffold)',color:'var(--cosmos-text-primary)'}}><header className="h-16 border-b px-4 flex items-center justify-between" style={{background:'var(--cosmos-card)',borderColor:'var(--cosmos-divider)'}}><div><strong className="text-lg">OTYA AI</strong><span className="text-xs ml-2 opacity-60">by PeterSmart Link</span></div><div className="flex items-center gap-3"><button onClick={newChat} className="text-sm">New chat</button><a href="/download/otya-player" className="text-sm font-bold">Get OTYA</a></div></header><section className="flex-1 overflow-auto"><div className="max-w-3xl mx-auto px-4 py-8 space-y-6">{messages.length===0&&<div className="min-h-[60vh] grid place-items-center text-center"><div><div className="w-16 h-16 rounded-2xl grid place-items-center mx-auto text-3xl font-black" style={{background:'rgba(123,97,255,.16)',color:'#a99cff'}}>O</div><h1 className="text-3xl font-black mt-4">How can OTYA help?</h1><p className="mt-2 text-sm opacity-60 max-w-lg">Ask about playback, downloads, updates, accounts, privacy or troubleshooting. Never send passwords or verification codes.</p><div className="grid sm:grid-cols-2 gap-2 mt-6">{['Why is my video not playing?','How do I update OTYA?','Where are my downloads?','I cannot verify my account'].map(x=><button key={x} onClick={()=>setInput(x)} className="rounded-xl border p-3 text-left text-sm" style={{background:'var(--cosmos-card)',borderColor:'var(--cosmos-divider)'}}>{x}</button>)}</div></div></div>}{messages.map((m,i)=><div key={i} className={`flex ${m.role==='user'?'justify-end':'justify-start'}`}><div className="max-w-[88%] rounded-2xl px-4 py-3 text-sm whitespace-pre-wrap" style={{background:m.role==='user'?'rgba(123,97,255,.22)':'var(--cosmos-card)',border:m.role==='assistant'?'1px solid var(--cosmos-divider)':'none'}}>{m.content}</div></div>)}{busy&&<div className="text-sm opacity-60">OTYA AI is thinking…</div>}{error&&<div className="text-sm text-red-400">{error}</div>}</div></section><footer className="p-4"><div className="max-w-3xl mx-auto rounded-2xl border p-2 flex gap-2" style={{background:'var(--cosmos-card)',borderColor:'var(--cosmos-divider)'}}><textarea rows={1} value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} className="flex-1 resize-none bg-transparent outline-none px-3 py-2" placeholder="Message OTYA AI…"/><button onClick={send} disabled={busy||!input.trim()} className="cosmos-button rounded-xl px-4 font-bold">Send</button></div><p className="text-[10px] opacity-50 text-center mt-2">Conversation memory is stored on OTYA servers. OTYA AI may make mistakes; sensitive actions require verified support.</p></footer></main>}
