# OTYA Command Center route

`/admin/ai` is a protected Admin surface inside the signed-in Otya Space environment.

Administrators use the normal Otya account sign-in. The server resolves Admin eligibility from that same identity and requires the configured owner verification when needed. There is no separate Admin username/password or browser-entered admin token.

All privileged APIs remain server-authorized independently of client navigation or UI visibility.
